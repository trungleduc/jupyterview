import  RemoteFileBrowser  from "../vtk_components/filebrowser"
import {createTestModel} from "./utils"

describe("Test VtkModel", () => {

    it('should be createable', () => {
      expect(1).toEqual(1)
    });

});
